$(document).on('pagecreate', '#command-home', function(event){
	$(document).on('click', '.edit-profile', function(){
		$('#command-edit-profile').popup('open');
	});

	$(document).on('click', '.add-location', function(){
		/* .popup('open') */
	});
});