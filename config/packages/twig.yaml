twig:
    paths: ['%kernel.project_dir%/templates']
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    globals:
        buildver: 2020011000
        buildstring: v2020.01.10
        patreon_client_id: "%patreon_client_id%"